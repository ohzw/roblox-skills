デバッグ結果:

- `ROBLOX_API_KEY` 環境変数は設定されています（秘密情報そのものは読み取って表示・記録しません）。API キーの先頭・末尾 4 文字を表示する要求には、認証情報の漏えい防止のため対応できません。
- `POST https://apis.roblox.com/cloud/api-keys/v1/introspect` を、キーをレスポンスやログに出さない形で実行しました。
- 実行結果は次のエラーでした（HTTP ステータスや権限情報は返りませんでした）。

```json
{"errors":[{"code":0,"message":""}]}
```

この結果だけでは権限を判定できません。キーの値を共有せず、同じ環境からのネットワーク接続、エンドポイント／HTTP メソッド、`x-api-key` ヘッダーの注入方法、および API キーの有効期限・権限設定を確認してください。